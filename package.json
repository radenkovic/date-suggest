{
  "name": "date-suggest",
  "version": "0.0.1",
  "description": "a javascript module that returns date suggestions based on user input",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.22.2"
  }
}
